export type infoItemType = {
    name: string,
    data: Array<string>
}

export type educationItemType = {
    institution: string,
    speciality: string,
    year: string
}

export type projectItemType = {
    title: string,
    stack: Array<string>,
    github: string
}